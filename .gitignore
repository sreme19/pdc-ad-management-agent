config.local.yaml

__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/

.DS_Store

# Competitor reference media — third-party creative pulled from public posts.
# Deliberately NOT committed: these are other companies' copyrighted assets, the
# store's own README marks everything here NOT SHIPPABLE, and the durable record
# is the prose in research/notes/ plus the shortcodes in sources-*.md, which make
# every file re-fetchable. Markdown in this tree IS tracked — only media is ignored.
creatives/_competitor-reference/**/*.png
creatives/_competitor-reference/**/*.jpg
creatives/_competitor-reference/**/*.jpeg
creatives/_competitor-reference/**/*.webp
creatives/_competitor-reference/**/*.gif
creatives/_competitor-reference/**/*.mp4
creatives/_competitor-reference/**/*.mov

# Downloaded source video/audio and derived frames (yt-dlp / ffmpeg / whisper).
# Re-pull with the commands in creatives/_competitor-reference/sources-*.md
*.wav
*.m4a
*.info.json

# Creative production sources (creatives/<slug>/_source/).
# The frames, clips, audio generations and Flow project exports that fed a finished
# asset. Kept on disk because Flow and Grok are non-deterministic and none of it can
# be regenerated — but deliberately NOT committed: it is ~100MB per video creative of
# binaries git can never shed once taken. The durable record is the README.md in each
# _source/, which IS tracked and describes what every folder holds.
creatives/*/_source/*
!creatives/*/_source/README.md

# Archived raw generations (creatives/_archive/).
# Batches of Grok/Flow output pulled off a session and parked rather than sorted —
# 74 files and ~29MB in the first one alone. Same reasoning as the _source rule
# above and the same failure it prevents: git cannot shed a binary once it has
# taken it, and nothing references these. They escaped that rule only because they
# sit under _archive/ rather than under a creative's own _source/.
# Ignored by extension rather than wholesale, so the README.md describing each
# batch stays tracked — as with _competitor-reference, the durable record is the
# prose, and the files themselves are non-regenerable but also unused.
creatives/_archive/**/*.jpg
creatives/_archive/**/*.jpeg
creatives/_archive/**/*.png
creatives/_archive/**/*.webp
creatives/_archive/**/*.gif
creatives/_archive/**/*.mp4
creatives/_archive/**/*.mov
